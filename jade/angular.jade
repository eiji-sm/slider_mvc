!!!
html(lang="ja", ng-app)

  head
    meta(charset="utf-8")
    title Slider MVC

    link(rel="stylesheet", href="css/raphaelicons.css", media="screen")
    link(rel="stylesheet", href="css/common.css", media="screen")

    script(src="js/lib/modernizr.js")
    script(src="https://ajax.googleapis.com/ajax/libs/jquery/1.8.2/jquery.min.js")
    script(src="js/lib/angular.min.js")
    script(src="js/demodata.js")
    script(src="js/angular-slider.js")

  body
    #wrapper(ng-controller="mainCtrl")
      header
        h1 Angular Demo
      section#mainSlider
        ul.slideContainer
          li.slideContent(ng-repeat="data in dataList")
            img(ng-src="image/{{data.url}}", width="640")
        .arrows
          span.icon.arrowLeft(ng-click="decrease('slideNumber')", ng-class="{ac: slideNumber != 0}") <
          span.icon.arrowRight(ng-click="increase('slideNumber')", ng-class="{ac: slideNumber != dataLength}") =
        nav.slideNavi
          ul
            li.slideNaviList(
              ng-repeat="number in dataNumbers",
              ng-click="eq('slideNumber', number)",
              ng-class="{ac: slideNumber == number}"
            )
              img(ng-src="image/{{dataList[number].url}}", width="110")
      section#mainContent
        .slideDescription.ac
          h2 {{dataList[slideNumber].title}}
          p {{dataList[slideNumber].description}}